
version: '1.0'
steps:



  itai_test:
    title: Launch composition
    type: launch-composition
    environment_name: "itai-env"
    composition: 
      version: '3'
      services:
        mongo:
          image: alpine:latest
          command: echo 'hello ${{CF_BUILD_ID}}'
   
